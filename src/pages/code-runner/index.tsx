import { type NextPage } from "next";
import AppShell from "~/components/ui/AppShell";

const CodeRunnerPage: NextPage = () => {
  return <AppShell>code runner</AppShell>;
};

export default CodeRunnerPage;
